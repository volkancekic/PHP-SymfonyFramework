{% extends 'base.html.twig' %}

{% block title %}Alışveriş Sepeti{% endblock %}

{% block body %}
    <h1>Alışveriş Sepeti</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shopcart.id }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ shopcart.userid }}</td>
            </tr>
            <tr>
                <th>Productid</th>
                <td>{{ shopcart.productid }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ shopcart.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shopcart_index') }}">Listeye Geri Dön</a>

    <a href="{{ path('shopcart_edit', {'id': shopcart.id}) }}">Düzenle</a>

    {{ include('shopcart/_delete_form.html.twig') }}
{% endblock %}
